# Development environment variables
GOOGLE_CLOUD_PROJECT=astranauts-461014
ENVIRONMENT=development

# Optional: OAuth2 credentials (if not using credentials.json)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Optional: Setia module configuration
SETIA_RISK_DATA_BUCKET_NAME=astranauts-risk-data

# Optional: Ollama configuration for Sarana module
OLLAMA_API_BASE_URL=http://localhost:11434

# Optional: API configuration
PORT=8080
HOST=0.0.0.0

# Optional: Logging configuration
LOG_LEVEL=INFO
